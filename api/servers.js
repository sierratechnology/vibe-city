import {RedisStore} from '../server/cloud-store.js';import {catalog} from '../server/catalog.js';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'Creating servers is disabled.'});try{const raw=await new RedisStore().read(),room=raw?JSON.parse(raw):null;res.status(200).json(catalog(Object.values(room?.leases||{}).filter(l=>l.until>Date.now()).length));}catch{res.status(503).json({error:'Server directory temporarily unavailable.'});}}
