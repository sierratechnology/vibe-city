export const BUTTONS=['Cross ✕','Circle ○','Square □','Triangle △','L1','R1','L2','R2','Create','Options','L3','R3','D-pad up','D-pad down','D-pad left','D-pad right'];
export const DEFAULT_BINDINGS={build:3,primary:7,rotate:6,previous:4,next:5,use:0,jump:11,sprint:10,light:12,eat:13,storage:14,map:15};
export const ACTION_LABELS={build:'Toggle construction',primary:'Context action / attack / interact / place',rotate:'Rotate building',previous:'Previous belt slot / piece',next:'Next belt slot / piece',use:'Use selected belt slot',jump:'Jump',sprint:'Sprint (hold)',light:'Toggle flashlight',eat:'Eat meal',storage:'Open nearby locker',map:'Open map / terminal'};
export function deadzone(value,threshold=.18){return Math.abs(value)<=threshold?0:Math.sign(value)*(Math.min(1,Math.abs(value))-threshold)/(1-threshold);}
export function createGamepadReader(){let previous=[],identity=null;return{read(pad,enabled=true){const connected=pad?.connected&&pad.mapping==='standard',identityNow=connected?`${pad.index}:${pad.id}`:null;const buttons=connected?pad.buttons.map(b=>!!b.pressed):[];const pressed=buttons.map((v,i)=>enabled&&identity===identityNow&&v&&!previous[i]);previous=buttons;identity=identityNow;return{connected:!!connected,buttons:enabled?buttons:[],pressed,axes:enabled&&connected?pad.axes.slice(0,4).map(v=>deadzone(v)):[0,0,0,0]};}};}
export function loadBindings(storage){try{const saved=JSON.parse(storage.getItem('vc-controller-bindings'));if(saved){const candidate=Object.fromEntries(Object.keys(DEFAULT_BINDINGS).map(k=>[k,saved[k]??DEFAULT_BINDINGS[k]]));if(Object.values(candidate).every(v=>Number.isInteger(v)&&v>=0&&v<16&&![1,8,9].includes(v))&&new Set(Object.values(candidate)).size===Object.keys(DEFAULT_BINDINGS).length)return candidate;}}catch{}return{...DEFAULT_BINDINGS};}
