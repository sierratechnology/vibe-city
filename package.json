{
  "name": "vibe-city",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "node --disable-warning=ExperimentalWarning server/index.mjs --dev",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "node --disable-warning=ExperimentalWarning server/index.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "three": "^0.181.2",
    "typescript": "^5.9.3",
    "vite": "^7.2.6"
  },
  "devDependencies": {
    "@types/three": "^0.184.1"
  }
}
