export async function sendVerification(email,url){if(!process.env.RESEND_API_KEY||!process.env.MAIL_FROM)throw Error('Email delivery is not configured yet. Your account is saved; verification will be available shortly.');const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:'Bearer '+process.env.RESEND_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.MAIL_FROM,to:[email],subject:'Verify your Vibe City email',text:'Confirm your email to enter Vibe City. This link expires in 30 minutes.\n\n'+url+'\n\nIf you did not request this, ignore this message.'}),signal:AbortSignal.timeout(10000)});if(!response.ok)throw Error('Verification email could not be sent. Please try again shortly.');return true;}
export async function sendRecovery(email,url){if(!process.env.RESEND_API_KEY||!process.env.MAIL_FROM)throw Error('Password recovery email is not configured.');const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:'Bearer '+process.env.RESEND_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.MAIL_FROM,to:[email],subject:'Reset your Vibe City password',text:'Use this link to reset your Vibe City password. It expires in 30 minutes.\n\n'+url+'\n\nIf you did not request this, ignore this message.'}),signal:AbortSignal.timeout(10000)});if(!response.ok)throw Error('Password recovery email could not be sent.');return true;}
